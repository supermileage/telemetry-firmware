{
    // unit test launch configuration:
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Unit tests",
            "program": "${workspaceFolder}/test/bin/test",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}